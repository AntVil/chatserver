#presentation infp: 
-10-15min
-20% of grade
-chat (frontend + backend)

#design choices:
-constant requests to stay logged in (prevent people being in list of online users)

#problems:
-storing messages -> to text-document with seperation characters
-special html characters in messages -> replacing with HTML encodings